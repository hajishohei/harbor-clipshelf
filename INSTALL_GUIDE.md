# HarboR ClipShelf インストール手順（社員向け）

所要時間は5分ほどです。

**いちばん簡単な方法：** 次のページを開くと、お使いのパソコン（Mac / Windows）を自動で判別して、貼り付ける1行を表示します。

https://hajishohei.github.io/harbor-clipshelf/

## Mac の場合（ターミナルに1行貼るだけ・おすすめ）

1. **ターミナル** を開きます（Spotlight（⌘Space）で「ターミナル」と入力して Return）。
2. 次の1行をコピーしてターミナルに貼り付け、Return を押します。

   ```
   curl -fsSL https://raw.githubusercontent.com/hajishohei/harbor-clipshelf/main/scripts/install-mac.sh | bash
   ```

   最新版のダウンロード →「アプリケーション」フォルダへの配置 → 起動まで自動で行います（1〜数分）。「開発元を確認できません」などの警告は出ません。
3. 「はじめに」の画面が開きます。**「アプリに直接貼り付ける」の「許可する」** を押し、システム設定の「アクセシビリティ」で HarboR ClipShelf を ON にしてください（Paste と同じく、Return で元のアプリにそのまま貼り付けるために必要です）。
   - 同じ画面の **「フォルダへのアクセスをまとめて許可」** も押しておくと、「デスクトップ／書類／ダウンロードのファイルにアクセスしようとしています」という確認がフォルダごとに出なくなります（開いた画面の「＋」で HarboR ClipShelf を追加して ON）。
   - Paste を使っていた方は、同じ画面の **「Paste から取り込む」** でコピー履歴とピンボードを移せます。
4. 「使ってみる」を押せば準備完了です。画面右上のメニューバーにもアイコンが出ます。

すでにブラウザからダウンロードして「開いていません」と表示された場合も、上の1行を実行すれば入れ替わって起動します。

### ターミナルを使わない場合

ダウンロードページ（https://github.com/hajishohei/harbor-clipshelf/releases/latest ）の **Assets** から `HarboR-ClipShelf-（版）-mac-universal.dmg` をダウンロードします。

1. .dmg をダブルクリックし、出てきたアイコンを「アプリケーション」フォルダへドラッグします。
2. 「アプリケーション」フォルダから **HarboR ClipShelf** を開きます。
   - 「"HarboR ClipShelf" は開いていません」「Appleは…を検証できませんでした」と表示されたら、**「完了」** を押します（「ゴミ箱に入れる」は押さない）。
   - **システム設定 → プライバシーとセキュリティ** を開き、下の方にある「"HarboR ClipShelf" は…ブロックされました」の横の **「このまま開く」** を押し、パスワードを入れてもう一度 **「このまま開く」** を押します（macOS 15以降はこの方法だけが使えます）。
   - macOS 14以前なら、アプリを **右クリック →「開く」** でも開けます。
3. 以降は上の手順 3・4 と同じです。

## Windows の場合（PowerShell に1行貼るだけ・おすすめ）

1. スタートボタンを右クリック →「**ターミナル**」（または「Windows PowerShell」）を開きます。
2. 次の1行をコピーして貼り付け（Ctrl+V または右クリック）、Enter を押します。

   ```
   irm https://raw.githubusercontent.com/hajishohei/harbor-clipshelf/main/scripts/install-windows.ps1 | iex
   ```

   x64 / ARM を自動で判別し、管理者権限なしでインストールして起動します。「PC が保護されました」の警告は出ません。
3. 「はじめに」の画面で「使ってみる」を押せば準備完了です。画面右下（タスクトレイ）にもアイコンが出ます（見当たらないときは「^」をクリック）。

### コマンドを使わない場合

ダウンロードページ（https://github.com/hajishohei/harbor-clipshelf/releases/latest ）の **Assets** から、ほとんどのPCは `HarboR-ClipShelf-（版）-win-x64.exe`、Snapdragon 搭載PCなど ARM 版の Windows は `win-arm64.exe` をダウンロードしてダブルクリックします。「Windows によって PC が保護されました」と出たら **「詳細情報」→「実行」** を押し、画面の指示どおりに進めます。

## 使い方（Paste・Yoink と同じです）

### コピー履歴（Paste）

| やりたいこと | Mac | Windows |
|---|---|---|
| コピー履歴を開く | ⇧⌘V | Ctrl+Shift+V |
| 選んで貼り付け | ← → で選んで Return（ダブルクリックでも可） | ← → で選んで Enter |
| 書式なしで貼り付け | ⇧Return | Shift+Enter |
| 左から○番目をすぐ貼り付け | ⌘1〜9 | Ctrl+1〜9 |
| 探す | そのまま文字を入力 | そのまま文字を入力 |
| よく使うものを固定 | カードを上のピンボードへドラッグ（右クリック →「固定」でも可） | 同左 |
| コピーした順に連続で貼り付け | メニューバーのアイコン →「Paste Stack」→ 貼り付けたい場所で ⌘V を繰り返す（ショートカットは設定で割り当て可） | タスクトレイのアイコン →「Paste Stack」→ Ctrl+V を繰り返す |

- 画面の下にカードが横に並んだバーが出ます。選んで Return を押すと、**元のアプリにそのまま貼り付けます**。
- 画像の中の文字や、コピー元のアプリ名でも探せます。
- パスワードもコピー履歴に残ります（残したくない場合は 設定 → プライバシー）。

### シェルフ（Yoink）

- ファイルをドラッグし始めると、**画面の左端にシェルフが出てきます**。そこに置いておき、あとでシェルフからドラッグして取り出します。
- 置いてマウスを離すと、シェルフは**細いタブになって画面の右端へ移動**します。タブにマウスを乗せると開きます（移動先は設定 → シェルフ で変更できます）。
- 複数のファイルをまとめて置くと1つの「スタック」になります。右下の数字を押すと中のファイルが並び、1つずつ選んだり取り出したりできます。チェックボタンで複数選択もできます。
- 目のボタン（または Space）でプレビューが開きます。ほかの場所をクリックすると閉じます。
- サブモニターなど別のディスプレイで作業すると、シェルフもそちらに移動します。
- 取り出すと Finder / エクスプローラーと同じく「移動」になり、シェルフからは消えます（残したいときは鍵ボタン）。
- 表示 / 非表示は **F5**（Windows は Ctrl+Shift+Y）。2回続けて押すと、クリップボードの内容をシェルフに置きます。
- ドラッグ中にシェルフを出したくないときは **fn**（Windows は Alt）を押したまま。

設定（メニューバー／タスクトレイのアイコン →「設定…」）で、ショートカット・表示位置・保持期間など、すべて変更できます。

## 必要な人だけONにする機能（設定）

| 機能 | できること | Mac で追加で必要な許可 |
|---|---|---|
| 画面から文字を読み取る | 範囲を選ぶだけで、画像や動画に写った文字をコピー | 画面収録 |
| ウィンドウ整列 | ⌃⌥←→ / Ctrl+Alt+←→ などでウィンドウを半分・3分の1に並べる | アクセシビリティ、オートメーション |
| マウスでフォーカス自動切替 | マウスを乗せたウィンドウが前面に来る | アクセシビリティ、オートメーション |
| スリープ防止 | AIの作業中などにスリープさせない（Caps Lock連動も可） | なし |

Mac で許可を求められたら、**システム設定 → プライバシーとセキュリティ** の該当項目で「HarboR ClipShelf」をONにしてください。設定 →「アクセス権」に「許可する」ボタンもあります。

> システム設定で ON になっているのに「許可が必要です」と出る・直接貼り付けができないときは、ClipShelf の **設定 → アクセス権 →「許可をやり直す」** を押し、開いた一覧で HarboR ClipShelf を ON にしてください（古い許可を消してから設定し直します）。画面の文字読み取りが効かないときは「画面収録」の一覧から HarboR ClipShelf をいったん削除（−ボタン）して、もう一度追加してください。

## アップデート（新しい版が出たとき）

新しい版が公開されると、画面に **「新しい版があります」** とお知らせが出ます。メニューバー（タスクトレイ）のアイコンの **「新しい版 … にアップデート」**（または 設定 → アップデート）を押すだけで更新できます。もう一度ダウンロードし直す必要はありません。

- **Mac**：自動でダウンロードして入れ替わり、数十秒で新しい版が起動します（「アプリケーション」フォルダ以外に置いている場合は、開いた画面でアプリを「アプリケーション」へドラッグしてください）。
- **Windows**：インストーラが開くので、「次へ」で進めてください。
- Mac で更新後に「アクセシビリティの許可をやり直してください」と出たら、「許可をやり直す」を押して、開いた一覧で HarboR ClipShelf を ON にしてください。

## よくある質問

- **⇧⌘V を押しても何も出ない（Mac）** 本家の Paste が起動していると、同じキーを取り合って反応しません。ClipShelf がお知らせを出すので、Paste を終了してください（設定 → ショートカット の「Paste を終了」ボタンでも終了できます）。
- **F5 を押してもシェルフが出ない（Mac）** MacBook などでは fn キーを押しながら F5 を押してください。
- **パスワードもコピー履歴に残る？** はい、初期設定では 1Password などからのコピーも残ります。残したくない場合は、設定 → プライバシー →「機密情報を無視する」を ON にするか、「次のアプリケーションを無視する」にそのアプリを追加してください。メニューの「一時停止」で記録を一時的に止めることもできます。
- **データはどこに保存される？** 自分のパソコンの中だけです。コピー履歴などがインターネットに送られることはありません（通信するのは、新しい版が出ていないかの確認と、コピーしたリンクのページ名・画像の取得だけです。後者は 設定 → プライバシー で OFF にできます）。自分の複数のパソコンで共有したい場合は、設定の「同期フォルダ」に Google Drive などの同期フォルダを指定してください。
- **うまく動かないときは？** 設定 →「情報」の「ログを開く」で開いたフォルダの `main.log` を、このツールの管理担当者に送ってください。
